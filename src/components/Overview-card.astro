---
interface Props {
	title: string;
	body: string;
	img: string;
}

const { href, title, body } = Astro.props;
---

<div class="container card bg-dark d-flex">
<h1>{title}</h1>
<p>{body}</p>

</div>

<style>
    .card {
        height: 300px;
        width: auto;
    }
</style>